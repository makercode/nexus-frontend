import { SubdomainDirective } from './subdomain.directive';

describe('SubdomainDirective', () => {
  it('should create an instance', () => {
    const directive = new SubdomainDirective();
    expect(directive).toBeTruthy();
  });
});
